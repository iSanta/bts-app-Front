import { Convergence, Pacifico} from 'next/font/google';

export const convergence = Convergence({subsets:['latin'], weight: '400'});
export const pacifico = Pacifico({subsets:['latin'], weight: '400'});
